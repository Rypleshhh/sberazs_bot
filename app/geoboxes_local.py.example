# Скопируйте этот файл в app/geoboxes_local.py и впишите свои координаты.
# app/geoboxes_local.py в .gitignore — никогда не попадёт в git/GitHub.

GEOBOXES: dict[str, tuple[float, float, float, float]] = {
    # "example_city": (37.3, 55.5, 37.9, 55.9),
}
