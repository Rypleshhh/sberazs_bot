services:
  scraper:
    build:
      context: .
      dockerfile: Dockerfile.scraper
    restart: unless-stopped
    env_file:
      - .env.scraper
    ports:
      - "8080:8080"   # порт API, наружу — только для лёгкого сервера, см. README
    volumes:
      - ./data:/app/data
